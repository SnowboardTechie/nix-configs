[project]
name = "hindsight-env"
version = "0.1.0"
description = "Locked runtime environment for the Hindsight API service (see modules/services/hindsight.nix)"
requires-python = ">=3.12,<3.13"
dependencies = [
    "hindsight-api==0.9.2",
]
